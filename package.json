{
  "name": "stocktaking-system",
  "version": "2.0.1",
  "description": "Modern tablet-optimized stock-taking system",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
